/**
 * In the "app" directory we can create new paths, which we wanna handle as routes
 * by adding new folders & in those folders add the "page" file which returns a React component.
 * The name of the component doesn't matter.
 */

const AboutPage = () => {
  return (
    <main>
      <h1>AboutPage</h1>
    </main>
  ) ;
};

export default AboutPage;
